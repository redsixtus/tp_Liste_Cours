import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicprojetfull',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
